import { Metadata } from "next"

const metadata: Metadata = {
    title: "Pricing",
    description: "Pricing page",
}

const PricingPage = () => {
  return (
    <div>PricingPage</div>
  )
}

export default PricingPage