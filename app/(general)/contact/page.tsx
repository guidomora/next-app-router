import { Metadata } from "next"

const metadata: Metadata = {
    title: "Contact",
    description: "Contact page",
}

const ContctPage = () => {
  return (
    <div>ContctPage</div>
  )
}

export default ContctPage